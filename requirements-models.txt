# PC-only model export deps (torch/ultralytics). NEVER installed in Docker/Pi runtime.
# Default is CPU wheels (works on x86 + ARM/Pi for export prep). For CUDA export
# on an NVIDIA PC, instead use: pip install torch torchvision torchaudio
# --index-url https://download.pytorch.org/whl/cu124
torch
torchvision
torchaudio
ultralytics
onnx
onnxslim
